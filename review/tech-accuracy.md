# 제1관문 검토 보고 — Technical Accuracy Reviewer (검토자 2)

- 검토자: Technical Accuracy Reviewer (제1관문 사실성, 2번 검토자)
- 대상: `docs/MAIOS_단행본.ko.html` (전문 — 본문·표·그림 주석·SVG 내부 텍스트·코드 블록 포함)
- 검토 일자: 2026-07-18
- 적용 헌장: `review/review-charter.md` (공통 규칙 7항 및 저자 확정 논제)
- 범위: AI·소프트웨어 기술 개념의 통용 의미 정합성, 본문 서술과 `src/maios/` 실코드 동작의 정합성. 수치·날짜 사실 오류는 1번 검토자(fact-checker.md) 소관이므로 중복 지적하지 않음.

## 지적 사항

| # | 위치 | 지적(원문 인용 포함) | 근거 | 제안 | 심각도 |
|---|---|---|---|---|---|
| 1 | 6장 1절 "설치와 연결" | "여기엔 함정이 하나 있다. SDK가 없으면 시스템은 오류를 내는 대신 **조용히** 규칙 기반 폴백으로 내려간다" — 현재 코드에서 산출물 생성 경로의 폴백은 더 이상 조용하지 않다. 커밋 `89c18ef`(2026-07-18 09:34, 책 최종 docs 커밋 `f3dbd29` 09:17 **이후**)가 `task_executor.py`에 경고 로그를 추가했다: 프로바이더 실패 시 실패한 프로바이더명과 원인 후보(SDK 미설치·키·크레딧)를 명시한 warning을 찍고 나서 echo로 내려간다. | `src/maios/kernel/task_executor.py:73-81` `logger.warning("LLM provider %r failed (%s); falling back to echo output. If you configured a real provider, check that its SDK is installed and the API key and credit balance are valid.", ...)`. 파이썬 logging은 핸들러 미설정 시에도 WARNING을 stderr로 출력하므로 사용자에게 보인다. 단, 이해·성찰 경로(`cognitive_interpreter.py:82-87`)와 분해 경로(`goal_decomposer.py:33-36`)는 여전히 예외를 삼키고 조용히 폴백한다. | "조용히"를 현재 동작에 맞게 수정: 산출물 경로는 경고 로그 후 폴백, 이해·성찰·분해 경로는 여전히 무경고 폴백임을 구분해 서술. 6장 4절 "산출물이 목표 문장 그대로다" 행에도 "콘솔 경고 로그 확인" 안내를 추가하면 정합. | P1 |
| 2 | 2장 1절 "여섯 단계" | "마지막 단계인 학습에서 나온 교훈은 점선을 그리며 **다음 사이클의 관찰 단계로** 되돌아간다" — 코드상 교훈(improvement_points)이 재유입되는 지점은 관찰(observe)이 아니라 **이해 단계의 회상**과 **행동 단계의 실행 프롬프트**다. 관찰 단계는 세계 상태 갱신만 하고 교훈을 소비하지 않는다. 그림 1의 자체 주석("교훈이 다음 사이클의 회상·실행 프롬프트로 환류")은 정확하며, 본문 문장만 어긋난다. | 학습 단계는 교훈을 경험 노드로 저장(`cognitive_loop.py:271-276`)하고, 다음 사이클의 이해 단계 회상이 이를 회수(`cognitive_loop.py:157-167`, `memory_recall.py:8,53`). 과거 수행의 교훈은 `prior_lessons` 메타데이터로 실행 프롬프트에 주입(`agi_foundation.py:305,419-427` → `executive_brain.py:357-363` → `task_executor.py:53-59`). 관찰 단계(`cognitive_loop.py:134-145`)에는 교훈 소비 코드가 없다. | "다음 사이클의 관찰 단계로"를 그림 1 주석과 같은 표현("다음 사이클의 회상과 실행 프롬프트로")으로 정정. | P2 |
| 3 | 6장 3절 "안전장치와 주의점" | "**모든 심사는 감사 로그에 남는다**" — 심사가 AuditLog에 기록되는 것은 맞으나, CLI 경로에서 감사 로그는 경로 없이 생성된 메모리 전용 객체라 프로세스 종료와 함께 사라진다. 워크스페이스에 감사 로그 파일은 쓰이지 않는다(거버넌스 판정 요약은 pursuits.json의 governance 필드로만 남는다). 사용 안내 맥락에서 "남는다"는 영속 기록으로 읽히기 쉽다. | `GovernanceManager.evaluate`는 항상 `audit_log.record` 호출(`governance/manager.py:240-242`)하지만, CLI는 `GovernanceManager()` 기본 생성(`cli.py:51`) → `AuditLog()`의 `path=None`(`manager.py:52-56, 230-231`) → `_persist`는 path 없으면 즉시 반환(`manager.py:79-81`). 즉 디스크에 감사 로그 없음. | "모든 심사 결과는 수행 일지(pursuits.json)에 남는다"로 고치거나, 감사 로그가 현재 세션 한정임을 명시. (또는 코드 쪽에서 워크스페이스에 audit 경로를 연결하는 선택지 — 결정은 저자 몫.) | P2 |
| 4 | 6장 3절 "안전장치와 주의점" | "데이터가 밖으로 나가는 유일한 경로는 LLM 호출 시의 프롬프트(**목표와 회상된 기억 일부**)이며" — '유일한 경로가 LLM 프롬프트'라는 대전제는 코드와 일치하나(다른 네트워크 경로 없음), 괄호의 범위 규정이 실제 전송 내용보다 좁다. 프롬프트에는 목표·회상 발췌 외에 과거 수행 교훈, 프로젝트 앞 단계 산출물 발췌(하위목표당 400자), 상황 해석문, 세계 상태 수치가 들어가고, 성찰 프롬프트에는 방금 생성된 산출물 전문이 포함된 outcome 딕셔너리가 통째로 들어간다. | 실행 프롬프트: notes·recalled·lessons 블록(`task_executor.py:39-70`), notes는 앞 단계 산출물 400자(`agi_foundation.py:383`). 이해 프롬프트: 세계 상태 수치+회상(`cognitive_interpreter.py:40-51`). 성찰 프롬프트: `f"Outcome: {outcome}"`로 전체 outcome 전송(`cognitive_interpreter.py:64-72`). | 괄호를 "목표, 회상된 기억·교훈·앞 단계 산출물 발췌, 직전 산출물 등"으로 넓히거나 "워크스페이스에서 발췌된 내용"으로 포괄 서술. | P2 |
| 5 | 5장 2절 "지원팀과의 대화, 그리고 침묵" (마지막 문단) | "mock 프로바이더로는 루프 전체와 **목표 분해**, 회상, 정렬 판정까지 계속 돌려볼 수 있었다" — CLI의 mock 프로바이더(`--llm mock`, MockGPTClient)는 프롬프트를 그대로 되울리는 echo라서 분해기가 요구하는 목록 형식("- " 줄)을 만들지 못하고, 분해는 항상 None → 단일 하위목표 폴백으로 내려간다. 즉 mock으로 '돌려볼' 수 있는 것은 분해의 폴백 경로이지 분해 동작 자체가 아니다. 분해 로직 검증은 목록 형식을 돌려주는 스크립트된 테스트 더블로 이루어졌다. 이 책 6장 4절 표("프로젝트가 하위 목표 1개로 돈다 → LLM의 분해 응답이 목록 형식이 아님")가 스스로 이 사실을 인정하고 있어 내적으로도 긴장이 있다. 에필로그의 "목표 분해는 mock 프로바이더로만 검증됐다"도 같은 이유로 'mock'의 지시 대상(CLI mock vs 테스트 더블)이 흐리다. | `MockGPTClient.generate` = `f"Mock GPT response: {prompt}"`(`llm_provider.py:22-23`); `GoalDecomposer._parse`는 "- "/번호 줄만 인정, 2개 미만이면 None(`goal_decomposer.py:37-40, 66-81`); None이면 단일 목표 폴백(`agi_foundation.py:371`). 분해 검증은 ScriptedProvider가 "- Research the topic\n- Write the memo" 형식을 반환하는 테스트(`tests/test_goal_decomposer.py:8-17, 49-54, 70-76`). | 5장 문장에서 "목표 분해"를 빼거나 "목표 분해의 폴백 경로"로 한정하고, 에필로그는 "목표 분해는 목록 형식을 돌려주는 테스트용 가짜 프로바이더로만 검증됐다"로 정밀화. | P2 |
| 6 | 서지 노트 "기대고 있는 어깨들" | "여섯 단계의 인지 루프는 **Soar, ACT-R, BDI로 이어지는 고전 인지 아키텍처 전통**의 경량 실용화이고" — Soar·ACT-R은 통용 의미의 인지 아키텍처가 맞으나, BDI(Rao & Georgeff)는 통상 실천적 추론에 기반한 **에이전트 아키텍처/모델**로 분류되며 Newell 계열 '인지 아키텍처' 전통으로 묶는 것은 범주가 다소 어긋난다(서지 항목 10의 논문 제목도 "BDI **Agents**"). | 통용 분류: Soar(Laird)·ACT-R(Anderson)=cognitive architecture, BDI=agent architecture/practical reasoning model. 경미한 범주 혼동으로, 셋을 한 계보로 묶는 서술은 개론서 수준에서 드물지 않으나 이 책의 정확성 기준에는 못 미침. | "Soar, ACT-R 같은 고전 인지 아키텍처와 BDI 에이전트 모델의 전통" 식으로 범주를 분리. | P2 |

## 검사했으나 정합이 확인된 핵심 항목

- **인지 루프 6단계**: observe→understand→plan→act→reflect→learn 순서·명칭(`cognitive_loop.py:28-44, 298-303`), 그림 1의 단계별 주석(관찰=세계모델 갱신 134-145, 이해=회상+온톨로지 157-167, 계획=플래너 선택 178-189, 성찰=개선 보고 238-252, 학습=교훈·경험 축적 254-285) 모두 일치.
- **되먹임 팽창 서술(3장)**: 수행 기록이 전이·경험·장기 기억에 함께 저장(`cognitive_loop.py:260-276, 346-365`); 삽입당 전체 그래프 재기록(`graph.py:149, 455-473`)과 이차적 자동 연결(`graph.py:207-224`) — 두 결함의 기술 서술 정확. 차단 3종: ① compact 요약(상태·플래너·산출/오류 300자, `executive_brain.py:454-472`), ② 노드당 8,000자 캡(`graph.py:84,124`, `cognitive_loop.py:351`, `world_model.py:379`), ③ 내용성 소스 우선(회상 `memory_recall.py:8,52-58`; 연구 `research/engine.py:128-135` TYPE_PRIORITY: document·concept·evidence 0 > research_report 1 > reflection·experience 2) — 그림 3·4 서술과 일치.
- **한국어 검색**: 음절 바이그램("전쟁과"→{전쟁,쟁과}, `graph.py:545-548`), IDF(`graph.py:513-514`), TF 한 겹(1+log tf, `graph.py:521`) — "한글 바이그램 TF-IDF" 표현 및 CHANGELOG의 TF 동기("동점 시 긴 문서") 서술과 정합.
- **흡수(ingest)**: 마크다운 제목 단위·텍스트 문단 단위 분할(`ingestor.py:136-150`), HTML 태그 제거(113-117), cp949 자동 폴백(103-111), 디렉터리 재귀(51-56), 경로 기반 결정적 ID `DOC-sha1(path:index)`(87,96), bulk 일괄 영속화(62)·auto_link=False(98).
- **온톨로지 접합부**: `.maios/ontology.ttl` 자동 감지(`workspace.py:48-51`), rdflib 선택 의존·부재/파싱 실패 시 무확장 폴백(`ontology.py:71-85`), 라벨 이웃 인덱스=subClassOf·type·domain/range·라벨된 술어(`ontology.py:97-108`, 그림 7 주석과 일치), 공백 무시 라벨 매칭과 그로 인한 라벨 섀도잉 서술(`ontology.py:42,110-112` — 정규화 dict에서 "지휘관의도"/"지휘관 의도" 충돌) — 4장 3절의 원인 설명과 코드 일치. J1 회상 확장(`memory_recall.py:45-48`), J2 위험 라벨 이웃 → HIGH 상향(`agi_foundation.py:198-215`; "제한과충돌"이 실제 라벨로 존재, `examples/mission_command_intent_core.ttl:42`), J4 3치 판정 순서(제한 접촉∧미면책→CONFLICT, 과업/목적 지원→ALIGNED, 그 외→CHECK; `intent_alignment.py:121-136` — 그림 6 흐름과 정확히 동일), CONFLICT 목표의 HIGH 격상→PENDING_APPROVAL(`agi_foundation.py:275-277, 288`), intent.json 5필드(`intent_alignment.py:24-28`). 4장 예시 판정(교량 확보=ALIGNED, 민간 포격=CONFLICT)은 바이그램 매칭 임계 0.3으로 재현 가능함을 수식으로 확인.
- **거버넌스**: 고위험 키워드 deploy·delete·external·production(`manager.py:127`), 금지 키워드는 운영자 설정(기본 빈 목록, `manager.py:124`), BLOCKED/PENDING_APPROVAL 구분(`agi_foundation.py:288`), `--approve`/셸 `/approve` 재실행(`cli.py:112-127`, `shell.py:16,73`).
- **CLI·셸**: 출력 태그와 형식([MAIOS]/[governance]/[cycles]/[ontology]/[recall]/[understanding]/[alignment]/[lessons]/[output] 500자 미리보기/[status]/[artifact]/[memory] nodes= pursuits= — `cli.py:57-134`), align 출력([action]/[ontology]/[verdict]/[rationale] 등, `cli.py:271-282`), 셸 슬래시 명령 8종(`shell.py:12-21`), 옵션 표(--llm mock/openai/claude/gemini=`llm_provider.py:176-192`, --capability 반복=`cli.py:100-102`, --max-cycles 기본 3, --workspace 기본 .maios=`workspace.py:14`).
- **워크스페이스**: 5개 파일/디렉터리 구성과 GP-/PJ- 산출물 규약(`workspace.py:25-32, 115-129`, `agi_foundation.py:62,107`), 전부 평문 JSON/MD(암호화·잠금 없음 — "평문 저장·동시 쓰기 비안전" 서술 일치), 외부 네트워크 호출은 LLM 프로바이더 3종뿐(연구 엔진은 완전 오프라인, `research/engine.py` — 웹 수집기 없음).
- **에코 폴백**: 프로바이더 부재/실패 시 산출물=목표 문장 그대로(`executive_brain.py:367-371`) — 6장 4절 "echo 폴백" 행 및 "프로젝트가 하위 목표 1개로 돈다" 행의 기술 설명 정확.
- **기술 용어 일반**: 교착어·조사 서술(3장 4절), 트리플 정의(주어·관계·대상), RDFS/OWL, "이차적(quadratic)" 복잡도 표현, TF-IDF·역문서빈도, MemGPT("컨텍스트를 가상 메모리처럼")·Reflexion(교훈 재주입)·Generative Agents(기억 스트림·회상·성찰) 요약 — 통용 의미대로 사용됨(#6의 BDI 범주 건 제외).

---

총 6건 (P0 0 / P1 1 / P2 5)
